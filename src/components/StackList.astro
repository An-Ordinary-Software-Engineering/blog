---
import Stack from './Stack.astro';

interface Props {
    stacks: {
        iconRef: string;
        name: string;
    }[];
}

const { stacks } = Astro.props;
---

<div class="grid grid-cols-4 grid-rows-2 w-72">
    {stacks.map((s) => (
        <Stack info={s} />
    ))}
</div>