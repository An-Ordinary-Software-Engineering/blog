---
import SubTitle from "./SubTitle.astro"

interface Props {
    subTitle?: string;
}

const { subTitle } = Astro.props;
---
<section class="h-fit">
    {subTitle && <SubTitle>{subTitle}</SubTitle>}
    <p class="text-lg">
        <slot />
    </p>
</section>